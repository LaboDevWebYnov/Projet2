{% extends "FOSUserBundle::layout.html.twig" %}

{% block stylesheets %}
    <link rel="stylesheet" href="../../bundles/user/css/registration/style_checkEmail.css" type="text/css" />
    <link rel="stylesheet" href="../../bundles/user/css/registration/style_confirmed.css" type="text/css" />

    
{% endblock %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
    <div id="confirmed_message">
        <p>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
        {% if app.session is not empty %}
            {% set targetUrl = app.session.get('_security.' ~ app.security.token.providerKey ~ '.target_path') %}
            {% if targetUrl is not empty %}<p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>{% endif %}
        {% endif %}
    </div>
{% endblock fos_user_content %}